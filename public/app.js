const $=s=>document.querySelector(s),jobs=$("#jobs"),status=$("#status");const esc=s=>String(s||"").replace(/[&<>"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]));
function list(xs){return"<ul>"+xs.map(x=>"<li>"+esc(x)+"</li>").join("")+"</ul>"}
function card(j){return `<article class="card"><div class="top"><div><span class="source">${esc(j.source)}</span><h2>${esc(j.title)}</h2><p class="company">${esc(j.company)} · ${esc(j.location||"Location not stated")}</p></div><div class="scores"><div><strong>${j.roleMatch}%</strong><small>Role match</small></div><div><strong>${j.acceptanceMatch}%</strong><small>Acceptance</small></div></div></div><div class="cols"><div><h3>Responsibilities</h3>${list(j.analysis.responsibilities)}</div><div><h3>Acceptance</h3>${list(j.analysis.acceptance)}</div></div><details><summary>Slight mismatches / Blockers</summary><p>${j.analysis.blockers.length?esc(j.analysis.blockers.join(" ")):"No explicit blocker detected from the listing text."}</p></details><a class="view" href="${esc(j.url)}" target="_blank" rel="noopener">View job</a></article>`}
async function run(){status.textContent="Searching…";jobs.innerHTML="";const p=new URLSearchParams({q:$("#q").value,location:$("#location").value,remote:$("#remote").checked,limit:"50"});try{const r=await fetch("/api/jobs?"+p);const d=await r.json();if(!r.ok)throw Error(d.error||"Search failed");status.textContent=`${d.matches} matches · ${d.unique} unique listings checked`;jobs.innerHTML=d.results.length?d.results.map(card).join(""):'<div class="empty">No matching roles found. Try a broader role or location.</div>'}catch(e){status.textContent="Search error: "+e.message}}
$("#search").addEventListener("submit",e=>{e.preventDefault();run()});run();
